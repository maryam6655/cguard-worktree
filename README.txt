Project: C-Guard Frontend (React + Vite)

Description:
This is a React frontend project built using Vite.

Steps to run:

1. Open terminal in this folder
2. Run: npm install
3. Run: npm run dev


The app will run on localhost (usually http://localhost:5173)